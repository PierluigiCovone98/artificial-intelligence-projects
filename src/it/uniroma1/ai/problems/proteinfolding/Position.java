package it.uniroma1.ai.problems.proteinfolding;

/**
 * Pair of values representing coordinates of the 2D grid for the Protein Folding problem.
 * The methods "equals()" and "hashCode()" are overridden by default.
 */
public record Position(int x, int y) { }
